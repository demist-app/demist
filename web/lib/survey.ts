// Identifiers for the in-app survey (components/SurveyModal.tsx). Separate
// from the component so the recording provider can check eligibility
// without pulling the modal into its bundle.
export const SURVEY_ID = 'sean_ellis_2026_10'
export const SURVEY_DONE_KEY = `demist_survey_${SURVEY_ID}`
